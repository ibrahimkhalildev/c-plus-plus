#include <bits/stdc++.h>

using namespace std;

int main()
{

    int n = 5;

    // // The overall time complexity of the program is O(n log n)
    // for (int i = 1; i <= n; i++)
    // {
    //     if (builtin_popcount(i) == 1)
    //     {
    //         for (int j = 1; j <= n; j++)
    //             cout << i << j << endl;
    //     }
    // }

    // // The time complexity of the program is O(n log n)
    // for (int i = n / 2; i <= n; i++)
    // {
    //     for (int j = 1; j <= n; j = j + i)
    //     {
    //         cout << i << j << endl;
    //     }
    // }

    // // The time complexity of the program is approximately O(n log n)
    // for (int i = n / 2; i <= n; i++)
    // {
    //     for (int j = 1; j <= n; j = j * 2)
    //     {
    //         cout << i << j << endl;
    //     }
    // }

    // // The total time complexity of this code segment is O(n^3)
    // for (int i = 0; i < n; i++)
    //     for (int j = i; j > 0; j--)
    //         for (int k = j; k > 0; k--)
    //             cout << i << j << k <<"\n";

    // // The time complexity of this code segment is O(n^2).
    // for (int i = 0; i < n; i++)

    // for (int j = i; j > 0; j--)
    //     cout << i <<" "<< j;

    // //the time complexity of this code segment is O(log n)
    // int k = 1;
    // while (k <= n)
    // {
    //     cout << k << endl;
    //     k = k * 2;
    // }

    return 0;
}
